[pytest]
pythonpath = src
testpaths = tests
markers =
    unit: fast, isolated unit tests
    integration: tests that exercise multiple layers (DB, FastAPI client, etc.)
filterwarnings =
    ignore::DeprecationWarning
